/*          Array                   |       ArrayList
        1. fixed size               | 1. variable length
        2.store premitive datatype  | 2. store nonpremitive i.e the classes and apply the function on their variables
        directly             

        3. length -> property       | 3. size() -> function
 */

 //Syntex ->
    ArrayList<Integer> list = new ArrayList<>();
//           (class)  (class name)